import { Routes } from '@angular/router';
import { NavbarInterviewComponent } from './navbar-interview/navbar-interview.component';
export const routes: Routes = [
    {
        path: 'resume-parsing',
    },
    {
        path: 'home',
        component: NavbarInterviewComponent
    }
];

